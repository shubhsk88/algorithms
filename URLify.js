function URLify(str,length)
{
    for(let i=0;i<=length;i++)
    {
        if(str[i]=" ")
        {
            str[i]="%20"
        }

        

    }
    for(let i=length;i<str.length;i++)
    {

    }
}